apiVersion: v1
kind: Service
metadata:
  name: {{ include "remote-contro-niryo-ned-2.fullname" . }}
  labels:
    {{- include "remote-contro-niryo-ned-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remote-contro-niryo-ned-2.selectorLabels" . | nindent 4 }}
